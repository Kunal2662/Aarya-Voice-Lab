# `reports/` — Generated Reports

Local storage for generated reports (environment audits, validation
summaries, benchmark reports). Everything under this directory except
`README.md` is git-ignored — reports are run artifacts, not source.
