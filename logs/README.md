# `logs/` — Runtime Logs

Local storage for CLI/pipeline runtime logs. Everything under this
directory except `README.md` is git-ignored. Logs must never contain raw
audio, transcript text derived from private recordings, or credentials —
see `docs/SECURITY.md` for logging rules.
