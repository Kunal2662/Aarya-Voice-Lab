# `benchmarks/` — Benchmark Runs

Local storage for voice-model benchmark run outputs, conforming to
`schemas/benchmark.schema.json`. No benchmarks have been run — Phase 0
defines the framework/schema only (see `docs/BENCHMARKING.md`).

Everything under this directory except `README.md` is git-ignored: results
may reference private model/dataset versions and must stay local.
